model:
  path: ~/models/Qwen3-Embedding-0.6B

data:
  train_path: data/train.jsonl
  max_seq_length: 256

training:
  batch_size: 16
  lr: 2e-5
  epochs: 3

packing:
  enabled: true

distributed:
  dp_size: 2
  tp_size: 1

kernels:
  fused: true
